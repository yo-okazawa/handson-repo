bash -c '
<%= "export https_proxy=\"#{knife_config[:bootstrap_proxy]}\"" if knife_config[:bootstrap_proxy] -%>

#<global-configuration>

  <% CHEF_SERVER_NAME="chefserver.cloud-platform.kddi.ne.jp" -%>
  <% CHEF_SERVER_IP="198.18.0.14" -%>
  <% REPO_SERVER_NAME="chefrepo.cloud-platform.kddi.ne.jp" -%>
  <% REPO_SERVER_IP="198.18.0.15" -%>
  <% REPO_URL="https://#{REPO_SERVER_NAME}/packages/" -%>
  <% BOOTSTRAP_WGET_OPTION="--no-check-certificate" -%>
  <% SSL_VERIFY_MODE="" -%>
  CHEF_PACKAGE="chef-12.0.3-1.x86_64.rpm"

#</global-configuration>

#<regist hosts>

  <% CHEF_HOSTS_STR="#{CHEF_SERVER_IP} #{CHEF_SERVER_NAME}" -%>
  if cat /etc/hosts | grep -q "<%= "#{CHEF_HOSTS_STR}" %>" ; then
    echo "regist hosts: CHEF-hosts is already registered" > /dev/null
  else
    echo "<%= "#{CHEF_HOSTS_STR}" %>" >> /etc/hosts
  fi
  
  <% REPO_HOSTS_STR="#{REPO_SERVER_IP} #{REPO_SERVER_NAME}" -%>
  
  if cat /etc/hosts | grep -q "<%= "#{REPO_HOSTS_STR}" %>" ; then
    echo "regist hosts: REPO-hosts is already registered" > /dev/null
  else
    echo "<%= "#{REPO_HOSTS_STR}" %>" >> /etc/hosts
  fi

#</regist hosts>

distro=`uname -s`

if test "x$distro" = "xSunOS"; then
  if test -d "/usr/sfw/bin"; then
    PATH=/usr/sfw/bin:$PATH
    export PATH
  fi
fi

exists() {
  if command -v $1 &>/dev/null
  then
    return 0
  else
    return 1
  fi
}

<% if knife_config[:bootstrap_install_command] %>
  <%= knife_config[:bootstrap_install_command] %>
<% else %>

  if ! exists /usr/bin/chef-client; then
    PACKAGE_URL=<%= "#{REPO_URL}" %>$CHEF_PACKAGE
    BTW_OPTION=<%= "#{BOOTSTRAP_WGET_OPTION}" %>
    echo "Installing Chef Client..."
    if exists wget; then
      bash <(wget $BTW_OPTION $PACKAGE_URL -O /tmp/chef-client-package.rpm)
      rpm -Uvh /tmp/chef-client-package.rpm
    elif exists curl; then
      bash <(curl $PACKAGE_URL -O /tmp/chef-client-package.rpm)
      rpm -Uvh /tmp/chef-client-package.rpme
    else
      echo "Neither wget nor curl found. Please install one and try again." >&2
      exit 1
    fi
  fi

<% end %>

mkdir -p /etc/chef

cat > /etc/chef/validation.pem <<'EOP'
<%= validation_key %>
EOP
chmod 0600 /etc/chef/validation.pem

<% if encrypted_data_bag_secret -%>
cat > /etc/chef/encrypted_data_bag_secret <<'EOP'
<%= encrypted_data_bag_secret %>
EOP
chmod 0600 /etc/chef/encrypted_data_bag_secret
<% end -%>

<% unless trusted_certs.empty? -%>
mkdir -p /etc/chef/trusted_certs
<%= trusted_certs %>
<% end -%>

<%# Generate Ohai Hints -%>
<% unless @chef_config[:knife][:hints].nil? || @chef_config[:knife][:hints].empty? -%>
mkdir -p /etc/chef/ohai/hints

<% @chef_config[:knife][:hints].each do |name, hash| -%>
cat > /etc/chef/ohai/hints/<%= name %>.json <<'EOP'
<%= Chef::JSONCompat.to_json(hash) %>
EOP
<% end -%>
<% end -%>

cat > /etc/chef/client.rb <<'EOP'
<%= config_content %>
<%= "#{SSL_VERIFY_MODE}" %>
EOP

cat > /etc/chef/first-boot.json <<'EOP'
<%= Chef::JSONCompat.to_json(first_boot) %>
EOP

echo "Starting first Chef Client run..."

<%= start_chef %>'